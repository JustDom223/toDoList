
export default function createHeader(){
    // create elements
    const headerElement = document.createElement('header');

    // add classes and ids

    // 
    headerElement.textContent = 'This is the header';
    return headerElement
};